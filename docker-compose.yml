services:
  webapp:
    image: spring-webflux
    build:
      context: .
      dockerfile: Dockerfile
    container_name: naver-news
    env_file:
      - .env
    ports:
      - "3579:3579"
    restart: unless-stopped
    networks:
      - local-network

networks:
  local-network:
    driver: bridge
    name: resources_local-network
